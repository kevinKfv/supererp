# Postgres Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: optimus-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: optimus-config
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              value: "changeme" # In production, use k8s Secret
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: optimus-config
                  key: POSTGRES_DB
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: optimus-ai
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

---
# Redis Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: optimus-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: optimus-ai
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379

---
# Qdrant Deployment & Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qdrant
  namespace: optimus-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qdrant
  template:
    metadata:
      labels:
        app: qdrant
    spec:
      containers:
        - name: qdrant
          image: qdrant/qdrant
          ports:
            - containerPort: 6333
---
apiVersion: v1
kind: Service
metadata:
  name: qdrant
  namespace: optimus-ai
spec:
  type: ClusterIP
  selector:
    app: qdrant
  ports:
    - protocol: TCP
      port: 6333
      targetPort: 6333
